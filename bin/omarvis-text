#!/bin/bash

set -euo pipefail

PLUGIN_DIR=$(cd -- "$(dirname -- "${BASH_SOURCE[0]}")/.." && pwd)

if ! text=$(omarchy menu input "Omarvis command" --width 600); then
  exit 0
fi
[[ -n $text ]] || exit 0

exec "$PLUGIN_DIR/bin/omarvis-run" --text-only --message "$text"
